diff --git a/node_modules/@russellthehippo/honker-node/api.js b/node_modules/@russellthehippo/honker-node/api.js
index 5925a7f..0099e32 100644
--- a/node_modules/@russellthehippo/honker-node/api.js
+++ b/node_modules/@russellthehippo/honker-node/api.js
@@ -36,28 +36,41 @@ function aborted(signal) {
 }
 
 function abortPromise(signal) {
-  if (!signal) return new Promise(() => {});
-  if (signal.aborted) return Promise.resolve();
-  return new Promise((resolve) => {
-    signal.addEventListener('abort', resolve, { once: true });
+  if (!signal) return { promise: new Promise(() => {}), cleanup() {} };
+  if (signal.aborted) return { promise: Promise.resolve(), cleanup() {} };
+  let onAbort;
+  const promise = new Promise((resolve) => {
+    onAbort = resolve;
+    signal.addEventListener('abort', onAbort, { once: true });
   });
+  return {
+    promise,
+    cleanup() {
+      if (onAbort) signal.removeEventListener('abort', onAbort);
+    },
+  };
 }
 
 async function waitForUpdateOrTimeout(updateEvents, signal, timeoutMs) {
   if (aborted(signal)) return;
-  if (timeoutMs == null) {
+  const { promise, cleanup } = abortPromise(signal);
+  try {
+    if (timeoutMs == null) {
+      await Promise.race([
+        updateEvents.next().catch(() => undefined),
+        promise,
+      ]);
+      return;
+    }
+    const ms = Math.max(0, timeoutMs);
     await Promise.race([
       updateEvents.next().catch(() => undefined),
-      abortPromise(signal),
+      delay(ms),
+      promise,
     ]);
-    return;
-  }
-  const ms = Math.max(0, timeoutMs);
-  await Promise.race([
-    updateEvents.next().catch(() => undefined),
-    delay(ms),
-    abortPromise(signal),
-  ]);
+  } finally {
+    cleanup();
+  }
 }
 
 function unwrapTx(tx) {
